# Points sudo at the graphical password helper (bin/sudo-askpass, linked into
# ~/.local/bin) for every process in the user session. sudo only uses it when
# there is no terminal to prompt on, so sudo in a terminal is unchanged.
# ${HOME} is expanded by systemd's environment.d; read once at login.
SUDO_ASKPASS=${HOME}/.local/bin/sudo-askpass
